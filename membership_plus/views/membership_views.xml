<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="membership_attendance_tree" model="ir.ui.view">
        <field name="name">membership.attendance.tree</field>
        <field name="model">membership.attendance</field>
        <field name="arch" type="xml">
            <tree string="attendances"
                  decoration-danger="membership_state in ('none','canceled', 'old')"
                  decoration-bf="membership_state in ('none','canceled', 'old')"
                  decoration-success="membership_state in ('waiting','invoiced','paid')"
                  decoration-muted="membership_state in ('free')">
                <field name="member"/>
                <field name="date"/>
                <field name="membership_state"/>
            </tree>
        </field>
    </record>


    <record id="action_membership_attendances" model="ir.actions.act_window">
        <field name="name">Fréquentations</field>
        <field name="res_model">membership.attendance</field>
        <field name="view_mode">tree</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to add a new Member
            </p>
            <p>
                Odoo helps you easily track all activities related to a member:
                Current Membership Status, Discussions and History of Membership, etc.
            </p>
        </field>
    </record>

    <menuitem name="Fréquentations" id="menu_attendance" sequence="0" parent="membership.menu_association" action="action_membership_attendances"/>
</odoo>